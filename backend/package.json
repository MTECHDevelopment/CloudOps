{
  "name": "cloudops-backend",
  "version": "1.0.0",
  "description": "CloudOps - Backend AWS Lambda Functions",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "deploy:prod": "sam deploy --config-env prod",
    "local": "sam local start-api",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0",
    "@aws-sdk/client-sns": "^3.400.0",
    "@aws-sdk/client-ses": "^3.400.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.400.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "aws-sam-local": "^0.2.11"
  }
}
